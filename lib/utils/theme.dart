import 'package:flutter/material.dart';

final appTheme = ThemeData(
  useMaterial3: true,
  fontFamily: 'Karla', // подключим позже из assets/fonts
  colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF2CC796)),
);
